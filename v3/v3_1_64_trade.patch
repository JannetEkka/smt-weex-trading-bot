--- smt_nightly_trade_v3_1.py
+++ smt_nightly_trade_v3_1.py
@@ -95,1 +95,9 @@
-    time.sleep(GEMINI_CALL_DELAY - elapsed)
+    sleep_time = GEMINI_CALL_DELAY - elapsed
+    for i in range(int(sleep_time) + 1):
+        print(f"  [HEARTBEAT] Gemini Cooling... {int(sleep_time)-i}s")
+        sys.stdout.flush()
+        time.sleep(1)
+
+def check_profit_lock_v64(current_pnl, peak_pnl):
+    if peak_pnl >= 1.0:
+        fade_limit = peak_pnl * 0.60
+        if current_pnl <= fade_limit and current_pnl > 0.15:
+            return True, f"Rule 0 Profit Lock: Faded from {peak_pnl:.2f}% to {current_pnl:.2f}%"
+    return False, None
